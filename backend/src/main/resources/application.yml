spring:
  application:
    name: community-forest-mapping
  
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:h2:file:./cfm-db;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE}
    username: ${SPRING_DATASOURCE_USERNAME:sa}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    driver-class-name: org.h2.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        jdbc:
          batch_size: 20
          fetch_size: 50
    show-sql: false
  
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

server:
  port: 8080

logging:
  level:
    root: INFO
    com.cfm: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

app:
  name: Community Forest Mapping
  version: 1.0.0
  upload-dir: ${UPLOAD_DIR:./uploads}
  dem-cache-dir: ${DEM_CACHE_DIR:./dem_cache}
  export-dir: ${EXPORT_DIR:./exports}
  gis-service-url: ${GIS_SERVICE_URL:http://localhost:8001}
  gis-service-timeout: ${GIS_SERVICE_TIMEOUT:300}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
